# Specs Index

Tool: tool-neutral (codex, claude, grok, dsh, and any CodeNote-routed agent)

## Active Location

Use this directory for new CodeNote process docs. Standard non-requirement work uses one `task-card.md`. Controlled work adds `spec.md`, `plan.md`, `tasks.md`, `verify.md`, and `handoff.md` when needed. Add `changes.md` when delivery spans multiple files.

Existing product/integration tasks remain under [../../docs/tasks/](../../docs/tasks/). OpenSpec capability work remains under [../../openspec/](../../openspec/).

## Rules

- New tasks follow CodeNote process date grouping: `vibe/specs/<yyMMdd>/<HHmm-task-id>/`.
- Business changes must link before/after documents and identify authoritative current behavior.
- Run link validation for documentation-heavy changes.
- Current hub: [PROJECT_STATUS.md](PROJECT_STATUS.md).
