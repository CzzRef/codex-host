# codexhost Project Status

Tool: cursor
Date: 2026-09-03

## Purpose

Compact process hub for active AI work. This file routes current tasks to project docs without storing durable rules.

## Rule Links

- Project documentation: [../rules/documentation.md](../rules/documentation.md)
- Project knowledge: [../knowledge/README.md](../knowledge/README.md)
- Global process rules: [../../../../CzzProj/CodeNote/AiRef/VibePractice/Vibe_Rules/process/rules.md](../../../../CzzProj/CodeNote/AiRef/VibePractice/Vibe_Rules/process/rules.md#3-project-location)

## Current Focus

- upstream/main 41 条提交合并（2026-09-07，claude session，worktree `codex/260907-upstream-main-merge` 已快进集成回主检出 `czz-dev`）：起因是核验本机 worktree 是否都已并入——`git worktree list` 只剩主检出、四个历史 worktree 分支与 `main`、全部 `origin/*` 相对 `czz-dev` 均 `ahead=0`，唯一债务就是 `upstream/main` 的 41 条。merge base 正是上轮 v0.5.0 的落点 `1d021a6`，无需回溯更早分叉；126 文件 +16131/-902，13 处冲突全为 content 冲突。多数两侧都新增一律都留（设置导航合成 `连接 / 模型 / 账号 / 会话导入 / 更新 / 关于`）。三处不是「都留」：官方 Codex 额度让位给上游按账号的 `AccountRateLimits`（本地 `#mergeOfficialRateLimits` 已无调用点，删；单一 `#officialRequestBroker` 被 runtime 池取代，剩余调用改走 `#requestOfficial`）；**外部 Thread 的 steer 保留本地原生插入、拒收上游的停-等-起**——上游 `ExternalTurnSteering` 自述「不是原生 steer 能力」，本地实现是超集且四家 Harness 已真机验证，与用户「要插入不要排队/打断」的验收口径一致，也与同日落成的[自研功能清单](../../docs/czz-dev-自研功能清单.md) B-1「最高风险项」的规定动作一致；上游 Renderer 侧 `installRendererExternalSteering` 是被摘掉那半的配套，暂不接线，文件与测试留在树里等真机复核后取舍。上游的 `#beginExternalTurn` 重构留下并接进本地 `#startExternalTurn`。按清单 B-1 要求做了逐 adapter steer 核对，八家全部完好。合并 `9d49f3a`，任务卡 [tasks](260907/upstream-main-merge/tasks.md)。真机复核已做（2026-09-07，Desktop 26.901.51231 build 8109，`codexhost launch` 起 Desktop 21580 / CDP 58659）判定 **ok**：官方与外部 Thread 的 `live-check` 都走 `workspace` 路由、不进错误边界，Turn 头 `{x:437,y:47,w:736,h:41}` 两侧一致；CDP 只读回读设置页注册表为六项，`models`(model-pool) 与 `accounts`(accounts) 并存，验证「两侧都留」的解法；**决策 2 端到端验证**——scratch grok Turn `48dfb8ad…` 运行中 `--steer true` 插入后仍是同一 `turnId`、终态 `completed` 而非 `interrupted`，插入消息作为轮内 user item 落在两条 agent 消息之间，Grok 真的改行为（数到 12 即停并写 `STEER-OK`）。`live-check` 的 `impact` 判定来自 5 条 `unattributed` 异常，实测是 Desktop 自带 `app.asar`（该报错串在其中 4 处、本仓 0 处）的高亮器对历史会话里 `行:行:文件路径` 形态围栏的解析噪声，与本次合并无关。未了：决策 3（上游 Renderer 侧 steering 是否接回）需一次由输入框驱动的真机检查。

- 自研功能对照台账（2026-09-07，claude session，主检出 `czz-dev`）：为「上游后续若支持同一功能，如何判定保留还是让位」建立常驻依据。按 `upstream/main` `de24f83` 与 `czz-dev` `e1c0cb5` 实测差集，逐项核验后落成 [docs/czz-dev-自研功能清单.md](../../docs/czz-dev-自研功能清单.md)：7 组 30 项，判定口径 `local-only` / `upstream-partial` / `superseded-by-upstream`。支撑证据：`packages/adapters/` 上游七家无 cursor；`HostFileInput` / `historyRedo` / `thread/pin` / `hasUnreadTurn` / `renderer-turn-header` / `worktree/list` / `codexhost-bypass` / `claudeInstallationIdentity` 在 `upstream/main` 均为 0 处；`openspec/changes/` 本地 27 个、上游 13 个，差集 14 个变更包全部对应表内 B–E 组自研行。最高风险项是 B-1 各 Harness 原生 steer——`HarnessSession` 要求 steer 重载，上游重写 adapter 时漏补即编译失败（v0.5.0 的 DSH 已发生一次）。本轮只做核验与文档抽取，未改任何源码；C-5（thread pin）与 D-7（外部权限选择器/模型标签）缺 openspec 变更包，待补。

- upstream v0.5.0 合并与插件架构适配（2026-09-06，claude session，worktree `codex/260906-upstream-v050-merge` 已集成回主检出 `czz-dev`）：接收上游核心——`adapter-composition.ts` 及其测试按上游删除，host-runtime 改用 `harness-plugin-loader` 动态装载，不再直接依赖任何具体适配器包。czz-dev 对该模块的全部本地改动本就只有注册 cursor 的 9 行，所以适配的等价物是给 cursor 补一套与另外七个一致的插件形态（`manifest.json` / `src/plugin.ts` / `assets/icon.svg` / `./plugin` 导出），cursor 是唯一需要新建插件入口的适配器。34 处冲突全解，凡两侧都是新增的一律都留；DSH 因上游重写为 `legacy/` + `modern/` 两代且两代都不含 steer，把本地的 `turn.steer` 与 `session/title` 原生标题重新移植了回去——这不是可选项，`HarnessSession` 接口要求 steer 重载，不补连编译都过不了。途中清掉一处本地死代码（dsh 模型 label 附带的 `description`，而 `harnessModelSchema` 是 strict 且从无该字段）。合并 `342aead`，集成 `efebb2f`，任务卡 [tasks](260906/upstream-v050-merge/tasks.md)。真机 Renderer 注入复核尚未跑。

- Harness 输入侧附件契约 `add-harness-file-input`（2026-09-05 至 09-06，claude session，主检出 `czz-dev`）：起因是用户以为 Grok 不支持上传，实测与 Grok 无关——Host↔Harness 的轮次输入只有 `HostTextInput` 一种部件，能力矩阵里根本没有可声明附件的位，八个 Harness 全都传不了。契约走**宿主绝对路径**而非内联字节，关键理由是降级后仍然可用：路径能对只收纯字符串的 pi / omp / antigravity / DSH 降级成一行让 Agent 自己去读，内联 base64 在它们上只能被丢弃。三级投递 `native` / `path-text` / `rejected` 明令禁止静默丢弃。四个 native 适配器各按自己协议转换（ACP `resource_link`、Anthropic base64 `image` / `document`、OpenCode URL 形态 `FilePartInput`），claude-code 是唯一需要读盘的，正好印证路径契约的用意。历史与自主轮次同步放宽，Codex UI 投影把文件部件渲染成同一行降级文本而不猜测未经验证的图片内容形态。Host 侧校验 `validateHostFileInputs()` 已落在 `harness-adapter` 并接进 harness-broker 的派发前。[change](../../openspec/changes/add-harness-file-input/proposal.md)、[tasks](../../openspec/changes/add-harness-file-input/tasks.md)。未了：进程内路径待有附件生产者时接线、真机带附件轮次未跑。

- Composer worktree + Cursor follow-ups (2026-09-05, claude session, main checkout `czz-dev`): four fixes driven by live use. `New worktree…` now prefills a complete `yyMMdd-core` name from the draft prompt (`suggestWorkspaceWorktreeName`, shared by every Harness) instead of asking for the core; a picked Host-managed worktree is no longer discarded when Desktop's run-location drifts back to `worktree`, which is why Threads started in the project root; workspace chips replaced their native `title` with an overlay tooltip, since the OS delay read as the chip being slow; the delegation Skill also installs into `~/.cursor/skills`, the only skill root Cursor reads, which is why Cursor Threads could never rename themselves while other Harnesses could. Commits `f1a6403`, `b3cd6ef`, `cc95430`. Live-verified so far: Cursor's empty-catalog path shows `Default model` on a cold start (the branch the upstream merge fixed but earlier rounds never reached). Still to verify on the running Desktop: worktree pick survival, the prefilled name, and Cursor renaming its own Thread (needs one more relaunch so the Skill lands).

- Shared Permission Mode vocabulary + Cursor bypass (2026-09-05, claude session, main checkout `czz-dev`): every Harness named its Modes differently (`bypassPermissions` / `always-approve` / `yolo` / `allow` / `dangerously-skip-permissions`) and Cursor had no bypass at all, so the Composer menu changed shape per agent and Cursor asked for approval every time. `HarnessPermissionMode` gains an optional `canonical` (`plan` / `ask` / `auto` / `bypass`) — native ids stay the only wire value, so existing Threads are untouched — and the picker renders the four shared choices in a fixed order, labelled `shared · native`, disabling the ones a Harness lacks with the reason instead of hiding them. Cursor gets a synthetic `codexhost-bypass` answered by the Host on ACP `session/request_permission`; no `--force` / `--yolo` / `--trust` is added and the choice is per-Thread. Commits `7cd5ca2` (vocabulary + Cursor bypass), `dddef13` (Turn header workspace chip hover detail). Rules in [czz-dev.md](../../docs/czz-dev.md).

- Transcript Turn header (2026-09-03 16:31, claude session, worktree `codex-conversation-display-f54032` fast-forwarded from `czz-dev dd25c65`): a pinned, opaque, Composer-aligned header at the top of the transcript replaces the hover `⋯` chip, the floating Turn action cluster and the bottom workspace bar. Row 1 = `第 N/M 轮` + the viewport-derived current Turn's prompt once its bubble scrolls out + Edit / Rollback / Redo on that Turn; row 2 = the workspace surface (core chip, touched roots behind `+N`, downward file disclosure with `本轮` tags, preview between header and Composer). Commits `5eca7b8` / `d7b96eb` / `4668afe` / `cbfc9c2` (rebased onto `c852197`), live-measured 2026-09-03 20:33 on the source relaunch (Desktop 26.831 PID 15137): geometry, reservation, prompt pinning, scrolling index, rollback + Redo on a scratch Pi Thread all verified; four live findings fixed in `891e4fe` (history-gap placeholder, `data-user-message-bubble`), `18b4a46` (‹ › turn arrows for short transcripts) and `00510e0` (inspect `turnIds` so rollback counts Host Turns, not Desktop's virtualised window; stale-transcript notice for legacy Threads) — those three were re-checked live on 26.901.22334. A 2026-09-04 live audit against the raw requirement then found and fixed five fidelity defects (duplicate prompt in the transparent chrome band, current-Turn probe line so the prompt button and `›` keep their Turn, single-row header when the conversation has no changed files, `Turn N/M` from Host `turnIds` instead of the virtualised DOM window, chevron only when the line clipped); the header is now 41px instead of 73px. Tracked in [spec](260903/1631-transcript-turn-header/spec.md), [raw](260903/1631-transcript-turn-header/raw-requirement.md), [changes](260903/1631-transcript-turn-header/changes.md).
- Worktree surface overhaul (2026-09-03 09:29, cursor session): static audit found all three Worktree surfaces incomplete; user confirmed at 10:05 and all four slices were implemented the same day on `czz-dev` (one commit per slice): (1) bottom bar mounts on `document.body`, always shows the core cwd chip, groups files per root with `+/-` and `+N` overflow, resolves outside paths through inspect `extraPaths` → `external` roots, larger interactive preview, Item-level file sets; (2) Host inspect publishes `rollback: { lastTurn, multiTurn }`, Turn actions disable with a reason, Edit refills the Composer when there is no native pencil, hover `⋯` chip + rAF repositioning, copy no longer promises file rewinds; (3) `codexhost/workspace/worktree/list|create` + Host `git worktree add -b codex/yyMMdd-x`, desktop-control rewrites `thread/start.cwd`, the checkbox became the "Worktree ▾" picker; (4) live Threads roll back any number of trailing Turns at their own Native Checkpoint with a whole-Session Redo slot, and paginated Threads get `thread/reverted` after rollback / Redo. Live measurements (bar offset, transcript re-read, per-Harness checkpoint fork) are still owed; see [plan §8](260903/0929-worktree-surface-overhaul/plan.md#8-execution-journal) and [raw](260903/0929-worktree-surface-overhaul/raw-requirement.md).
- Status: Host last-turn Redo is committed. The 2026-09-02 Turn-action cluster and the bottom workspace bar are superseded by the Turn header (2026-09-03, `6513243` / `98fe202`); Redo still enables only from Host `historyRedoAvailable` (official Threads keep the local flag from a rollback this session made), Edit confirms only when later Turns exist, and official Desktop Redo is a fallback solely for Codex-owned Threads. Preview remains [composer-overlay.preview.html](../../packages/renderer-extension/src/composer-overlay.preview.html), now showing the header.
- Latest task docs: [spec](260901/2042-external-thread-redo/spec.md), [raw](260901/2042-external-thread-redo/raw-requirement.md), [changes](260901/2042-external-thread-redo/changes.md), OpenSpec [add-external-thread-last-turn-redo](../../openspec/changes/add-external-thread-last-turn-redo/proposal.md).
- Existing product/integration work remains under [../../docs/tasks/260831-czz-dev-integration/](../../docs/tasks/260831-czz-dev-integration/spec.md).
- Sibling tasks now documented: Composer workspace-bar slice 6 (sibling worktrees / `additional` roots) in the workspace-bar OpenSpec change; Launcher source-checkout delegation CLI fallback in [task card](260902/1312-launcher-source-checkout-cli/task-card.md). The unread-only `app-server-host` hunk belongs to the unread change and is committed separately.
- Sibling task (claude session, 2026-09-02 14:12): delegation CLI `thread archive|unarchive` so EyPc can archive extra processes through the Host instead of the official app-server; see [task card](260902/1412-delegation-thread-archive/task-card.md). Live after the 15:22 source relaunch.
- Worktree checkbox routing is merged into `czz-dev` (`c9ee09b`): the checkbox drives Desktop `setComposerMode(worktree|local)`, and the workspace surface is the compact changed-file line. Follow-up (2026-09-02 19:30, this-commit in the control checkout): the preference now defaults to Local and persists only from the checkbox, because the default-checked preference plus persisted observed modes had forced every new chat into a new Worktree. Goes live after a Terminal `codexhost launch` relaunch. Tracked in [task card](260902/1340-worktree-checkbox-routing/task-card.md).
- In-turn interjection items (2026-09-02): `HostItem.userMessage` implemented end to end — Grok / Pi / OMP / DeepSeek Harness / Claude history fold, all five harnesses emit the live item, Pi/OMP settlement back to exactly one prompt Entry; Desktop reload check remains. Tracked in [task card](260902/1437-in-turn-interjection-items/task-card.md).
- Delegation permission mode (2026-09-02 19:10): `codexhost delegate start --permission-mode <mode-id>` takes a `harness inspect` permission-mode id so a CLI child (no Desktop approver) no longer ends `interrupted` on its first protected tool call; user ruling: an interrupted Turn with reply text stays 待继续, never unread. Tracked in [task card](260902/1910-delegation-permission-mode/task-card.md).
- Grok steer + archived listing (2026-09-02 14:00): grok 1.0.13 has no `x.ai/interject`; the Adapter now calls `_x.ai/interject` with `{sessionId, text}`, settles on checkpoint identity after waiting for the persisted `turn_completed`, and the Grok `1 + deliveredInterjections` relaxation from `2155d93` is withdrawn. `codexhost thread list` gains `--archived true|false` and an `archived` field on external rows so EyPc can retire Desktop-archived Threads. Tracked in [task card](260902/1400-grok-interject-archived-list/task-card.md) and OpenSpec [add-delegation-thread-list-archived](../../openspec/changes/add-delegation-thread-list-archived/proposal.md).
- Cursor native-default draft (2026-09-03 10:25, worktree `claude/cursor-multi-agent-codex-5be354`; **merged into `czz-dev` 2026-09-05 as `644e69b`**): Cursor could not be used from the Desktop Composer because its cold `inspect` has no Model catalog (`models: 0`) and the Renderer treated an `empty` catalog as not ready (send disabled, no draft carrier). Fix: an `empty` catalog means the Harness native default Model; the carrier `codexhost/cursor-native@@<mode>` carries a Permission Mode without a Model. The rebase onto `czz-dev` (144 commits, across upstream v0.4.4 and the Turn header) narrowed that rule to `externalAgentUsesNativeDefaultModel(agent)` — Cursor only — so Claude Code's same-Host empty catalog stays terminal and still blocks submission. Tracked in [task card](260903/1025-cursor-native-default-draft/task-card.md).
- Cursor native history (2026-09-02 15:02): replace ACP live-only with Grok-style `cursor-agent acp` plus `~/.cursor/acp-sessions` resume/snapshot. Merged into `czz-dev` (`467d491` / `d9809a5`). Tracked in [spec](260902/1502-cursor-native-history/spec.md).
- Side Chat parent binding (2026-09-02 16:49): ephemeral derived Side Chat stays bound to the source Thread; list omits those rows; projectless Fork no longer opens the child in the sidebar. Tracked in [spec](260902/1649-sidechat-parent-binding/spec.md).

## Active Task Index

| Task | Status | Authoritative Doc | Verification | Notes |
| --- | --- | --- | --- | --- |
| Transcript Turn header | `implemented / live-verified`; CDP attach live on 26.901.22334 (`4b45876`); `2d8a381` live: external Thread opens, `live-check --open external` `ok`; 22-Turn scratch Thread: gap placeholder excluded, arrows step, header at y=46; 2026-09-04 live audit fixed five fidelity defects and re-verified them on the running Desktop; only the `turnIds` rollback-count confirmation is still unread (user was driving Desktop) | [spec](260903/1631-transcript-turn-header/spec.md), [raw](260903/1631-transcript-turn-header/raw-requirement.md), [changes](260903/1631-transcript-turn-header/changes.md), OpenSpec [add-composer-workspace-bar](../../openspec/changes/add-composer-workspace-bar/tasks.md) §13 | `npm run typecheck` pass; `npm run lint` (eslint + boundaries) pass; renderer vitest 35 files 244 pass; Playwright `renderer-composer-workspace-surface` 1 pass; `npm run build:renderer` pass; per commit | Replaces hover `⋯` chip, floating cluster and bottom bar. Live: header `y=46` under the 46px app-shell header, `x=846 w=736` = Composer, rect stable over four scroll positions; column-reverse scroller with own padding, reservation 49px; bubble is `[data-user-message-bubble]`; long transcripts are virtualised (22-Turn Thread → 15 DOM nodes at the top), so `M` is the loaded count and rollback now counts from Host `turnIds`; legacy Threads keep a stale transcript after rollback (notice added). 2026-09-04: the app-shell chrome band is transparent, so the prompt pins on leaving the transcript viewport, not on passing the header; the current-Turn probe line sits 24px below the header so a Turn the header scrolled to stays current; the workspace row exists only with changed files and the core chip yields to a pinned prompt. Still `[待真机]`: paginated `thread/reverted` re-read, Stop label, native edit DOM, the two-row workspace state on a Thread with live file changes; Side Chat sharing one scroller mounts no header |
| Worktree surface overhaul | `implemented / partially live-verified`; slices 1–4 committed, picker project-root fix `this-commit`; UI layer (slices 1–2) superseded by the Turn header | [plan](260903/0929-worktree-surface-overhaul/plan.md), [raw](260903/0929-worktree-surface-overhaul/raw-requirement.md), OpenSpec [add-composer-workspace-bar](../../openspec/changes/add-composer-workspace-bar/tasks.md) §11–§12, [add-external-thread-multi-turn-rollback](../../openspec/changes/add-external-thread-multi-turn-rollback/proposal.md) | `tsc -b` pass; vitest host-runtime + mapping-store + renderer-extension 63 files 588 pass (slice 3 full run 189 files 1640 pass); Playwright `renderer-composer-workspace-surface` pass; eslint + boundaries pass; live 2026-09-03 11:28 on source Desktop PID 11131: bar left edge = Composer left edge (0px), hover `⋯` lands at Turn top-right | Live found the draft picker could not resolve the project root before the first `thread/start` (Desktop keys `executionTargetOverride.cwd` / `gitRootForStartingState` were not in `PATH_PROP_KEYS`); fixed + built; user screenshot 11:46 then showed the action cluster covering prompt text with see-through chips and the bar hiding the last transcript lines — cluster/`⋯` now prefer the gutter right of the Turn, chips are opaque, both hide during Desktop's native edit mode, and the bar reserves transcript bottom padding. All of this is built but the running controller keeps the old Renderer source, so it goes live only after a user-authorized normal quit + `codexhost launch`. Paginated transcript re-read on `thread/reverted` and per-Harness self-checkpoint fork still `[待真机]`; `openspec` CLI not installed |
| Host last-turn Redo | `committed b939a81 3b0275d` | [spec](260901/2042-external-thread-redo/spec.md) | vitest 79 files 712 pass; typecheck pass | OpenSpec change not archived |
| Composer overlay + Turn actions | `committed 2f3a53b`; superseded by the Turn header (`6513243` / `98fe202`) | [preview](../../packages/renderer-extension/src/composer-overlay.preview.html) | renderer vitest pass; Playwright composer surface 1 pass; typecheck pass | Cluster now sticks inside the conversation viewport (`turnActionPlacement`); Redo is thread-level from Host inspect; official Redo fallback only for Codex-owned Threads |
| Workspace-bar slice 6 | `committed 18cfa10` | [tasks](../../openspec/changes/add-composer-workspace-bar/tasks.md) | thread-workspace vitest pass | sibling worktrees + `additional` roots |
| Worktree checkbox routing | `integrated c9ee09b`; default-Local follow-up `this-commit` | [task card](260902/1340-worktree-checkbox-routing/task-card.md) | renderer workspace-bar + branch-toggle 10 pass; typecheck pass after merge; follow-up verification pending in task card | checkbox → Desktop `setComposerMode`; preference defaults to Local and persists only from the checkbox; awaiting relaunch |
| Claude catalog refresh | `committed 63d359e` | [task-card](260902/1352-claude-catalog-refresh/task-card.md) | adapter+command vitest 94 pass; typecheck/eslint/prettier; real 2.1.252→2.1.258 swap re-inspects without refresh | `claude update` 2.1.252→2.1.258 done on request; 18:50 relaunch verified live: Host 5909 returns `claude-fable-5-1` |
| Grok interject + archived list | `committed 5c459fd a3c9f60` | [task-card](260902/1400-grok-interject-archived-list/task-card.md) | vitest 187 files 1601 pass; typecheck pass; boundaries pass; live grok 1.0.13 probes | `_x.ai/interject`, settle waits for `turn_completed`, `thread list --archived`; live after 15:22 relaunch |
| Launcher controller reap | `committed 09e0a66` | [task-card](260902/1349-launcher-controller-reap/task-card.md) | cargo platform+launcher tests, clippy, vitest desktop-control 68 pass, parent-kill runtime proof | signal flag in Launcher + parent watch in Controller; SIGTERM end to end verified 19:15 (launcher 82835 → controller/Desktop/descriptor gone in 2 s, relaunched as 91215) |
| Launcher source-checkout CLI | `committed e12a5e8` | [task-card](260902/1312-launcher-source-checkout-cli/task-card.md) | cargo installation_layout 5 pass | delegation CLI fallback |
| Delegation thread archive | `committed a3c9f60`; round-2 `this-commit` | [task-card](260902/1412-delegation-thread-archive/task-card.md), OpenSpec [add-delegation-thread-archive](../../openspec/changes/add-delegation-thread-archive/proposal.md) | delegation-cli / control-server / registry vitest pass; app-server-host `archiv` filter pass; `tsc -b` pass | `codexhost thread archive|unarchive [<thread>]`; shares Desktop archive persistence + `thread/archived`; live after 15:22 relaunch; round 2: archive cascades to ephemeral side chats, `thread list` rolls a running side chat up into the source row (app-server-host `side chat|archiv` filter pass) |
| In-turn interjection items | `implemented / focused-verified / desktop-reload-pending` | [task-card](260902/1437-in-turn-interjection-items/task-card.md), OpenSpec [add-in-turn-interjection-items](../../openspec/changes/add-in-turn-interjection-items/proposal.md) | focused vitest grok/pi/projector/fake 101 pass; typecheck pass | `HostItem.userMessage` 五家实时+历史折叠；Desktop 视觉重载待做 |
| Delegation permission mode | `implemented / focused-verified / live-verified / eypc-foreground-pending` | [task-card](260902/1910-delegation-permission-mode/task-card.md), OpenSpec [add-delegation-permission-mode](../../openspec/changes/add-delegation-permission-mode/proposal.md) | coordinator + CLI vitest 38 pass; typecheck pass; live Grok `always-approve` probe `4cd13fa2` completed + Host unread | EyPc group placement awaits plugin foreground |
| AI rules init | `implemented-local / this-repo-commit-authorized` | [task-card](260901/2034-ai-rules-init/task-card.md) | `audit_ai_rules.py --mode project` OK | CodeNote catalog 另仓未提交 |
| czz-dev integration | existing / see task package | [docs/tasks/260831-czz-dev-integration](../../docs/tasks/260831-czz-dev-integration/spec.md) | see that verify | not migrated into `vibe/specs/` |
| Cursor native-default draft | `merged / focused-verified / live-partial`; on `czz-dev` as `644e69b` (2026-09-05) | [task card](260903/1025-cursor-native-default-draft/task-card.md), [changes](260903/1025-cursor-native-default-draft/changes.md) | post-merge on `czz-dev`: `npm run typecheck` pass; `npm run lint` pass; `vitest run --config tests/vitest.config.js` 210 files 1842 pass / 9 skipped; Playwright composer surface 1 pass; `npm run build:renderer` pass | Renderer `empty` catalog = native default **for Cursor only** (`externalAgentUsesNativeDefaultModel`); shared-contracts `@@<mode>` codec; 2026-09-05 17:4x live on the relaunched Desktop: one Cursor message routed to `harnessId: cursor` with `@agent` carried end-to-end, zero exceptions; the empty-catalog branch was NOT exercised (the label read `Auto`, so the catalog was already populated) and still needs a cold-catalog repro |
| Cursor native history | `integrated 467d491 / d9809a5` | [spec](260902/1502-cursor-native-history/spec.md) | cursor vitest 9 pass; tsc cursor+renderer; TS+renderer rebuilt | worktree removed, branch kept; Desktop quit but this agent cannot relaunch GUI — run `codexhost launch` in Terminal |
| Side Chat parent binding | `implemented-local / this-commit` | [spec](260902/1649-sidechat-parent-binding/spec.md), OpenSpec [add-sidechat-parent-navigation](../../openspec/changes/add-sidechat-parent-navigation/proposal.md) | vitest 2 files 14 pass | list omit ephemeral derived; Renderer stays on source; no cross-harness transcript inject |

## Verification State

- Last verified: 2026-09-02 (1437 focused vitest + typecheck; post-merge renderer workspace tests 10 pass + typecheck; earlier full vitest + typecheck + boundaries + cargo launcher + Playwright composer surface)
- Commands: `vitest run` 79 files 712 pass / 2 skipped; `npm run typecheck` pass; `node tools/check-boundaries.mjs` pass; `cargo test -p codexhost-launcher --bin codexhost installation_layout` 5 pass; Playwright `renderer-composer-workspace-surface` 1 pass
- Live Desktop 2026-09-02 15:22: source relaunch after a clean quit; launcher 25474 / desktop 25476 / controller 25478 / host runtime 25634; descriptor `0600`. Supersedes 13:25 (16647/16649) and 08:18. CDP measurement from 13:25 still stands for the Turn-action cluster.
- Unverified gaps: live rollback→Redo on a real external Thread (covered by Host tests only), `gate:*`, full `npm test`
- 2026-09-04 11:4x (Transcript Turn header live audit, Desktop 26.901.22334 launched by `codexhost launch` at 10:58, CDP 62118): read-only CDP probes plus `Page.captureScreenshot` on three Threads, then the rebuilt Renderer hot-reinstalled over CDP (`installRendererBinding` disposes the previous install; stale `<style>` tags dropped first because `ensureStyle` is install-once). Five defects found and fixed, then re-verified live: header 41px (was 73px) with rect `x=846 y=46 w=736 h=41` identical at eight scroll positions; prompt not repeated while the bubble is readable at `bubbleBottom` 11 / 29 / 48; `Turn 5/5` with 4 DOM Turns and `Turn 1/5` with 3 (Host `turnIds`); prompt click and both arrows keep their Turn past the 600ms override; first Turn clears the header at scroll-top (95 ≥ 87); single-Turn Thread hides the arrows. Gates: `npm run typecheck` pass, `npm run lint` pass, renderer vitest 105 files 772 pass, `vitest run --config tests/vitest.config.js` 206 files 1835 pass / 9 skipped, Playwright `renderer-composer-workspace-surface` 1 pass, `npm run build:renderer` pass.
- 2026-09-04 12:1x (26.901.22334, Host `2d8a381`): `npm run live-check:codex-desktop -- --open external` → verdict `ok` (launch +5 s, no Renderer exception, header `Turn 1/1` with prompt segment); scratch Pi Thread `8c87dcd5…` 22 Turns via delegation CLI: bottom `Turn 10/10` (10 DOM nodes), top `Turn 1/6` (7 nodes incl. 1 history-gap), `›` → `Turn 2/7`, header y=46 throughout; rollback-count confirmation not read (user input in Desktop interrupted the probe).
- 2026-09-04 11:3x (Desktop 26.901.22334 live): `codexhost launch` ready +5.2 s on 20858 and on 22334 (CDP port from Desktop argv, `/json/list` → `Runtime.evaluate`); official Thread: header `y=47`, `x=437 w=736` = Composer, `Turn 4/4`, arrows, Edit enabled, reserve `true`; external Thread: Desktop error boundary reproduced twice and root-caused via `Runtime.exceptionThrown` (`text_elements`), fix `2d8a381` (protocol-core vitest 12 files 96 pass, typecheck + lint pass) not yet running; `live-check:codex-desktop --open official` on the old Host reports `impact` with the projector owner hint.
- 2026-09-04 11:0x (upstream v0.4.4 merge `4b45876`): `npm run build:typescript` / `typecheck` / `lint` / `build` pass; `vitest run` 206 files 1833 pass / 9 skipped; `npm run test:rust` all crates pass (shim proxy 36 incl. legacy-migration cases, launcher 45 + 4); Playwright `renderer-composer-workspace-surface` 1 pass. Main checkout `czz-dev` fast-forwarded, `npm install` + `npm run build` pass. Live relaunch on Desktop 26.901 not yet run (user-started plain Desktop PID 30825 still open).
- 2026-09-03 21:3x (Transcript Turn header): `npm run typecheck` pass; `npm run lint` pass; renderer + shared-contracts vitest 47 files 339 pass; host `app-server-host` inspect/rollback case pass with `turnIds`; Playwright `renderer-composer-workspace-surface` 1 pass; `npm run build:renderer` pass. Live 2026-09-03 20:33 source relaunch: launcher 15134 / Desktop 15137 / controller shim 15594 / host runtime 15598; descriptor `0600`; read-only inspector probes on two official Threads and one scratch Pi Thread (archived). Supersedes the 18:1x line.
- 2026-09-02 14:09 (Grok interject + archived list): `vitest run` 187 files 1601 pass / 7 skipped; `npm run typecheck` pass; `node tools/check-boundaries.mjs` pass; live `_x.ai/interject` on scratch grok sessions (deleted). Host code is live after the 15:22 relaunch.
- Latest Sidecar result: main-thread
- Latest Prior Task Overlap: last-message-edit rollbackLastTurn; decision `new-task`
- Latest Documentation Impact: `requirement-canonical` OpenSpec change plus this hub
- Latest Efficiency / Token Evidence: `usage unavailable`

## Open Risk Or Deploy Gates

- **Gate (2026-09-04 11:3x): Desktop 26.901 (now 26.901.22334, build 7746, asar `sha256:30a90346…`, fuse block `0 1 0 0 1 1 0 0 1`) is attached over CDP after the `upstream/main` v0.4.4 merge (`4b45876`): `codexhost launch` reaches ready in ~5 s and the Renderer injection runs (overlay root present, Turn header verified on an official Thread).** 26.901's `app-initial` reads `text_elements.length` on every user text part; fork mid-Turn steer `userMessage` items lacked it, so opening an external Thread hit Desktop's error boundary ("hit a snag") — fixed in `2d8a381` (`packages/protocol-core/src/codex-ui-projector.ts`), built in the main checkout, **live only after the next user-authorised quit + `codexhost launch`**. `npm run live-check:codex-desktop -- --open official|external` (`6bf88c8`, [tools/codex-desktop-live-check](../../tools/codex-desktop-live-check/README.md)) now automates identify → fuse → launch/attach → CDP probe → owner-attributed verdict; any plain Desktop launch may auto-update again (`CODEXHOST_DISABLE_UPDATES` only covers codexhost-launched Desktops).
- Steer regressions found by live probes 2026-09-02 18:10 were fixed and committed (`d241c2e` / `7ca6ef0`): Pi/OMP history folds a steer by `stopReason: "toolUse"` or by its queued `message.timestamp`, and Claude `sdk-transport` settles by `command_lifecycle` instead of waiting for a second `result` (see [task card](260902/1400-grok-interject-archived-list/task-card.md) and [steer tasks](../../openspec/changes/add-external-turn-steer-queue/tasks.md) 1.9/1.10/2.5). The 19:15 relaunch (launcher 91215 / host 91347) runs the dist rebuilt with these fixes; UI steer / Side Chat visual check remains.
- Gate: `app-server-host.ts` mixes Redo, unread fallback, and workspace `extraRoots` hunks. The `renderer-turn-actions.ts` gate is resolved: `renderer-turn-header.ts` owns layout, `renderer-turn-action-controller.ts` owns Host-first Redo, `renderer-turn-actions.ts` keeps pure copy / helpers
- Blocking condition: resolved 2026-09-02 by patch-staged batches `3b0275d` / `18cfa10` / `43b6ca0`; overlay `2f3a53b` is the rollback point for the Turn-action redesign
- Worktree control: `codex/260901-composer-workspace-bar` worktree removed, branch `0cd7997` kept and contained by `czz-dev`/`origin/czz-dev`; `codex/260902-worktree-checkbox-routing` is contained by `czz-dev` via merge `c9ee09b` (child HEAD `142fcfb`). `codex/260902-cursor-native-history` is contained by merge `467d491` (child HEAD `d9809a5`).
- Rollback note: Redo is additive (`historyRedo` optional, Host method, Renderer Host-first with official click fallback)

## Governance Baseline

- Template propagation: accepted global baseline; this project keeps only project-specific routes and does not copy mother-board rules.
- Codex evolution: `v3-route-accepted`; no Hook, supervisor, or rollout change in this repository.
- Rule Task Trace: accepted global baseline; this initialization is project-local and does not add a CodeNote registry row.
- `w24-primary-objective-continuity-accepted`: primary user work remains ahead of advisory governance lanes.
- `w28-documentation-impact-accepted`: this round synchronized project-current adapters/hubs and CodeNote catalog.
- `w30-standard-requirement-owner-accepted`: Standard requirement ownership remains raw requirement plus the Spec owner. The current Redo task uses that owner; AI-rules-init remains Standard non-requirement.

## Memory Routing

- Task rule declaration: recorded on the task card
- Sidecar document route: main-thread
- Prior Task Overlap: wechat-download-api / starter-kit adapter shape
- Evolution Candidate: none
- Project rules: created this round from former root `AGENTS.md` / `CLAUDE.md`
- Knowledge: architecture created this round; glossary remains `docs/领域术语表.md`
- ADR: empty index only; OpenSpec stays the capability authority
- Error memory: one record — [Grok 插队后 Host Turn 被判 failed](../knowledge/error-memory/grok-interjection-persists-extra-native-turn.md#L1) (2026-09-02, verified; root cause corrected the same day: `x.ai/interject` Method not found plus settle-before-persist, superseding the `2155d93` relaxation, fix committed `5c459fd`; native steer and `thread send --steer true` committed `658e905` / `a3c9f60`, live after 15:22 relaunch)
- DB memory: not configured

## Cross-Repository Links

| Concern | Repository | Status Hub |
| --- | --- | --- |
| Rule kernel / catalog | CodeNote | [project-index.json](../../../../CzzProj/CodeNote/vibe/knowledge/project-index.json#L470) |
| Adapter shape reference | wechat-download-api | [wechat PROJECT_STATUS](../../../../wechat-download-api/vibe/specs/PROJECT_STATUS.md#L1) |

## Next Update Trigger

Update this hub when current focus, active task docs, verification status, open gates, sibling links, or memory routing changes.
